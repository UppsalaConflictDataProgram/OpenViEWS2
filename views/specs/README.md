# specs

ViEWS has many definitions that should be the same throughout the project:

* Models, which features go into which models?
* Periods, what are the time limits for training, calibrating and predicting?

This module provides a system of specfiles to use as references throughout.